import { createApp } from 'vue'
/* The line `// import './style.css'` is a comment in the TypeScript code. It is used to provide
information or explanations about the code but is not executed by the compiler. In this case, it
looks like it is a commented-out import statement for a CSS file named `style.css`. This means that
the CSS file is not being imported or used in the application at the moment. */
import './style.css'
import App from './App.vue'

createApp(App).mount('#app')
